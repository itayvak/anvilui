export type DesignTokens = {
    spacing: number
    font: Font
    radius: BorderRadius
}

export type Font = {
    main: string
    title: string
}

export type BorderRadius = {
    small: string
    normal: string
    large: string
    full: '1000px'
}
